#
# Appel pour une autre machine que la machine courante:
#
# make SERVER=foo

mk_graphes:
	./plot.sh "temps" 0
	./plot.sh "temps" 1
	./plot.sh "debit" 0
	./plot.sh "debit" 1


clean cleanxtra:
	rm -f script.plot
