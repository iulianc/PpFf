#
# Appel pour une autre machine que la machine courante:
#
# make SERVER=foo

mk_graphes:
	./plot.sh "WordCount" "temps" 0
	./plot.sh "WordCount" "temps" 1
	./plot.sh "WordCount" "debit" 0
	./plot.sh "WordCount" "debit" 1


clean cleanxtra:
	rm -f script.plot
